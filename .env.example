# Voxi live-tier credentials. COPY this to `.env.local` (gitignored) and fill in.
# NEVER paste these values into chat — just fill this file and tell me "go".

# ── GCP: ALREADY WORKING via the gcloud CLI (no ADC, no service-account key) ──────
# Auth is a bearer from `gcloud auth print-access-token`. Vertex Gemini + Cloud Vision are live.
# Leave these as-is unless you want a different project/region.
GCP_PROJECT=your-gcp-project
GCP_LOCATION=us-central1
GEMINI_MODEL=gemini-2.5-flash

# ── DEEP RESEARCH LAYER (reveal facts + encyclopedia-depth description + chat grounding) ──────────
# The researcher runs a bounded AI-SDK tool-loop: Claude drafts a cited dossier from Firecrawl web crawls, then
# the honesty gate verifies every fact's provenance (quote⊆source + quote⊨text). BOTH optional: absent, the loop
# falls back to Vertex Gemini Google-Search grounding on your existing gcloud auth (still real, just less deep).
FIRECRAWL_API_KEY=                   # firecrawl.dev → API keys. Deep web crawl for the research dossier.
ANTHROPIC_API_KEY=                   # console.anthropic.com → API keys. The Claude drafting brain (the eve model).

# ── GIVE ME THESE NOW: auth + voice ──────────────────────────────────────────────
# Clerk (auth). Create an app at dashboard.clerk.com → API keys.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
CLERK_JWT_KEY=                       # optional: PEM public key for fully networkless JWT verify

# ElevenLabs (Voxi's spoken voice + the 2-host podcast audio). elevenlabs.io → profile → API key.
ELEVENLABS_API_KEY=
ELEVENLABS_VOXI_VOICE_ID=            # optional: a dry British narrator voice_id (I can pick one from the library)

# Deepgram (STT for the live voice conversation). deepgram.com → API keys. Optional (else Gemini STT).
DEEPGRAM_API_KEY=

# ── LATER: full backend deploy (Cloud Run + Cloud SQL/AlloyDB, etc.) ──────────────
VOXI_URL_SIGNING_KEY=                # I'll generate a strong random one if left blank
# DATABASE_URL / EVE_SELF_URL are produced by the deploy scripts, not by you.

# ── LATER: iOS + App Store (needs your Mac + Xcode + an Apple Developer account) ──
# Subscriptions are StoreKit 2 DIRECT — NO billing vendor (RevenueCat removed). The server verifies
# transactions/notifications with Apple's PUBLIC root certs (no secret needed for verification). An App Store
# Connect API key is only needed to CALL the App Store Server API and comes with the Apple Developer setup.
APPSTORE_CONNECT_ISSUER_ID=          # App Store Connect → Users and Access → Integrations → Keys
APPSTORE_CONNECT_KEY_ID=
APPSTORE_CONNECT_PRIVATE_KEY=        # the .p8 contents (or a path)
# APPLE_TEAM_ID / bundle id are set during `eas build` on your Mac.

# ── Observability: structured logs + traces (PLAN §11). See docs/observability.md. ────────────────
# UNSET = structured JSON logs to stdout only (always on, needs no infra). Set the two OTLP vars to ship
# to a backend. Local stack: `docker compose -f infra/observability/docker-compose.yml up -d` then point at
# the collector. Hosted: Grafana Cloud (free tier) → Connections → OpenTelemetry gives you endpoint + token.
OTEL_EXPORTER_OTLP_ENDPOINT=         # LOCAL: http://localhost:4318 · CLOUD: https://otlp-gateway-<region>.grafana.net/otlp
OTEL_EXPORTER_OTLP_HEADERS=          # LOCAL: blank · CLOUD: Authorization=Basic <base64(instanceID:token)>
LOG_LEVEL=info                       # debug | info | warn | error | fatal
# Grafana MCP (lets Claude Code query logs/traces during an RCA — see .mcp.json):
GRAFANA_URL=                         # CLOUD: https://<org>.grafana.net · LOCAL: http://host.docker.internal:3000
GRAFANA_SERVICE_ACCOUNT_TOKEN=       # Grafana → Administration → Users and access → Service accounts → Add token
