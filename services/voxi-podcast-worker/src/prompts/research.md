Research the object "{{subject}}". Using web search, give 8–12 SPECIFIC, verifiable facts about it — a mix of what it IS (make/model/generation, year, key specifications, materials) AND what makes it a STORY: its origin, the people behind it, how it came to be, where it sits in history, and why it matters. Favor the concrete and the human over the generic; each fact must be the kind you could cite. Return ONLY a JSON array: [{"claim": "<one factual sentence>"}]. Output STRICT, valid JSON — never put a double-quote character inside a claim (use single quotes if you must quote a word). No prose, no markdown fences.
