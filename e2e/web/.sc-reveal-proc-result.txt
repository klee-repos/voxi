web E2E — reveal + processing states (real BFF + real NDJSON stream):
  PASS reveal-01: leads with a SPECIFIC title (the band hedge), not an empty/placeholder
  PASS reveal-01: confidence chip is band-colored from the real stream (data-band=PROBABLE)
  PASS reveal-01: title-led hierarchy — title comes BEFORE the photo thumb in the DOM order
  PASS reveal-02: exactly ONE primary action on the reveal card
  PASS reveal-02: CONFIDENT band → "How sure?" is hidden
  PASS reveal-03: PROBABLE → "How sure?" is auto-elevated (visible)
  PASS reveal-03: evidence panel hidden until asked, then reads as Voxi cross-checking sources
  PASS reveal-04: a real PROBABLE chip color was captured (warm gold band)
  PASS reveal-04: safety refusal shows a distinct caution treatment and NO confidence band
  PASS reveal-04: chip color ≠ safety-refusal color (gold vs caution are NOT the same)
  PASS reveal-05: "That's not it" opens the correction flow
  PASS reveal-05: correction is WRITTEN through the real BFF (POST /v1/tips → 200)
  PASS reveal-05: the card surfaces the server-decided disposition (not a client-faked banner)
  PASS proc-02: PARTIAL settles the chip to PROBABLE (not silently promoted to CONFIDENT)
  PASS proc-02: the streamed descriptive prose is shown in whatItIs (the model DID say something)
  PASS proc-02: the title is the HEDGE and was NOT mutated into the asserted token prose
  PASS proc-02: both candidate years are surfaced (disagreement shown, not collapsed silently)
  PASS proc-03: UNKNOWN routes to the INTERVIEW screen (not a reveal card)
  PASS proc-03: the first interview question + "why I ask" are rendered from the real BFF
  PASS proc-03: visibility toggle DEFAULTS to private (consent-before-global)
  PASS proc-03: answer + skip affordances present (interview is interactive, not a dead end)
  PASS proc-04: processing screen is up while the Guide consults (no premature reveal)
  PASS proc-04: a long-wait acknowledgement appears in-persona (no dead spinner)
  PASS proc-04: the stall STILL settles into a real reveal afterwards (not stuck)
  PASS proc-06: hard failure surfaces an in-persona failure state (not a reveal card)
  PASS proc-06: a "next action" (retry) is offered — the failure is recoverable, not a dead end
  PASS proc-06: the orb reflects the uncertain/failed state (orb.state=uncertain)

27/27 checks passed
WEB SC-REVEAL-PROC E2E GREEN
