web E2E — subscriptions/metering + a11y + safety (real BFF + framework PlaywrightDriver):
  PASS sub-01: baseline — 1 free scan reported by the real /api/v1/me settings surface
  PASS sub-01: a safety-refused scan surfaces the refusal (not a reveal)
  PASS sub-01: refusals do NOT count — 1 free scan still remains after the refused scan
  PASS sub-01: the still-available free scan reveals a real identification (not the paywall)
  PASS sub-01: cap mechanic — first real scan reveals (free scan honored)
  PASS sub-01: cap mechanic — burn remaining scans then next scan → in-persona paywall (real 402)
  PASS a11y-01: reduce-motion defaults OFF (no reduced-motion flag on the document)
  PASS a11y-01: toggling reduce-motion ON sets the document reduced-motion flag (anim/particle hook)
  PASS a11y-01: toggling reduce-motion OFF clears the flag again (preference is reversible)
  PASS a11y-03: before any turn, neither the spoken-turn marker nor the transcript is populated
  PASS a11y-03: a spoken (voice) turn writes BOTH a spoken-turn marker and a text transcript
  PASS a11y-03: a typed turn also pairs a spoken-turn marker with an advancing text transcript
  PASS safe: a pill/medical scan surfaces the distinct safety-refusal surface
  PASS safe: the refusal is NOT a confidence band — the gold chip is suppressed and carries no band
  PASS safe: the refusal surface is VISUALLY DISTINCT from the confidence chip (caution red ≠ warm gold)
  PASS safe: the refusal BLOCKS generation — no band, no candidate identifications, non-identifying title

WEB SC-SUBS-A11Y-SAFETY E2E GREEN
