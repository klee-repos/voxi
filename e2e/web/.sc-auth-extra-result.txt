web E2E — auth (returning session + protected redirect) + proc reconnect (real BFF + real NDJSON stream):
  PASS auth-02: a persisted session token authorises the protected /v1/me surface (no re-sign-in)
  PASS auth-02: the persisted session can take a protected action (capture) without an OTP exchange
  PASS auth-02: a fresh visitor first lands on welcome (sign-in required)
  PASS auth-02: after sign-in the returning user lands on the camera (not the OTP step)
  PASS auth-02: the authenticated chrome (tab bar capture CTA) is available, welcome is gone
  PASS auth-03: a signed-out visitor to a protected route lands on welcome (redirect)
  PASS auth-03: the protected screen (settings) is NOT shown to a signed-out visitor
  PASS auth-03: no authenticated chrome leaks pre-auth (the capture CTA is hidden)
  PASS auth-03: BFF rejects the protected /v1/me with 401 when signed-out (forged/missing session)
  PASS auth-03: the SAME protected surface passes once a real session is present (redirect ⇔ no session)
  PASS proc-05: the capture settled a reveal card on the first (online) connect
  PASS proc-05: dropping the network surfaces the offline banner (client observed the drop)
  PASS proc-05: a stream request while offline genuinely fails (the drop is real, not simulated)
  PASS proc-05: restoring the network clears the offline banner
  PASS proc-05: reconnecting via ?startIndex= resumes the SAME thread and loses no turns
  PASS proc-05: the reconnect targets the SAME durable thread row (resumes:true, same continuation)
  PASS proc-05: the reconnect is owner-scoped (a foreign principal is denied on reconnect too)

WEB SC-AUTH-EXTRA E2E GREEN (17 checks)
